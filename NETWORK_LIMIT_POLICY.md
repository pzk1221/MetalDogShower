# 网络请求与限流策略

## 核心原则

一次用户动作只产生完成该动作所需的最少请求。没有用户动作时没有后台轮询、WebSocket、周期任务或自动会话刷新。控制 POST 永不自动重放。

v0.2 没有硬编码“token 5 分钟 TTL”；token 是否有效只由官方响应判断。401 会结束本端会话并要求重新 OTP，不执行 refresh 或原请求重放。

## 本地单飞的范围

- `core` 的 `RequestGate` 串行控制流程，同类操作 2 秒去抖，所有控制 5 秒冷却。
- 手机主界面、小组件控制以及敏感的会话/路由变更共享手机 Application 级互斥锁。
- 小组件桥另拒绝同一手机进程中的第二个并发命令；`AppWidgetProvider` 自身只渲染/切换槽位，不执行网络或控制。
- 手表 Real 控制器有独立操作互斥锁；每次只能对当前明确选择的一个浴室执行操作。
- 手机锁与手表锁互不共享，服务端未发现 Idempotency-Key 或跨设备事务。因此这些保护**不能**防止两个设备同时操作；用户不得同时从手机和手表控制同一间浴室。

两个浴室是不同路由，但当前 repository 的 RequestGate 仍在各自进程内跨路由共享，以优先限制总体控制频率。

## 配置与 OTP

### 识别一间浴室

```text
1 × public device/info GET
```

每次只处理用户当前选中的槽位，不自动扫描另一间，不访问二维码的明文 URL，不自动重试。

### 请求验证码

```text
1 × validateCodeMark GET
1 × validateCode GET
```

仅由用户主动点击触发；无自动重试。界面有 60 秒本地再次发送冷却，这不是 token TTL。

### 登录

```text
1 × yzmLogin POST
```

用户主动提交一次；失败后由用户修正再试。验证码不持久化。服务端未提供已验证的 refresh 请求。

## 手机主界面

### 选择浴室或手动刷新

```text
1 × shower/info GET
```

选择会先丢弃该槽位的旧显示状态，再查询所选路由。失败时不猜测状态、不启用控制。

### 用户确认开启/关闭

每次手机控制都必须先做一次新鲜路由 GET；此前界面显示的状态不直接授权 POST：

```text
1 × shower/info GET（控制前）
  → 已是目标状态：0 × switch POST
  → 状态不同：1 × switch POST + 最多 1 × shower/info GET（结果确认）
```

所以一次改变状态的手机操作最大正常形态是 `2 GET + 1 POST`。控制前 GET 失败时可以证明没有发送 POST；POST 之后的超时/断连则按不明确结果处理，绝不自动再发 POST。

## 手机桌面小组件

- 切换“浴室1 / 浴室2”、小组件更新、主题变化和点击组件空白区域都不发网络请求。
- 小组件只展示手机主界面或已完成命令写入的非敏感最近状态；它不后台刷新。
- 控制入口要求所选浴室已配置、手机会话存在、状态已知且本地确认时间不超过 2 分钟，否则打开主应用。
- 点击“开启 / 关闭”只打开显式 `FLAG_SECURE` 确认 Activity；用户再次确认后才执行。
- 即使显示状态未超过 2 分钟，确认后仍执行一次新鲜 `shower/info GET`。已是目标状态时不发 POST；否则最多一次 POST 和一次确认 GET。
- 确认 Activity 一次生命周期只接受一次执行；重建时若此前处于执行中，会把结果标记为不明确而不会恢复为可再次点击。
- 手表是否已同步不是小组件/手机控制条件；只要求手机会话和该槽位配置有效。

## 手表

### 选择浴室并刷新

```text
1 × shower/info GET
```

进入或切换槽位会把可控制状态设为未知。只有明确选择了已绑定槽位并完成刷新后，手表才允许控制。

### 用户确认开启/关闭

```text
已知本地状态已是目标值：0 × POST
状态不同：1 × shower/switch POST + 最多 1 × shower/info GET
```

手表不会在一个 `setOpen` 调用中额外做控制前 GET，因此用户必须先完成显式刷新。切换浴室会使旧路由状态失效，不能把浴室1的状态拿来控制浴室2。

## 不明确结果

```text
1 × shower/switch POST
0 × 重复 POST
最多 1 × shower/info GET 进行结果核对
```

如果核对也失败，相关路由进入 UNKNOWN/不明确锁定态。用户必须现场确认，并在网络恢复后手动刷新；在成功刷新前不允许再次控制。取消/进程死亡若可能发生在 POST 已出站后，同样不能推断“未执行”。

## 重试矩阵

| 场景 | 状态 GET | 控制 POST |
|---|---:|---:|
| 连接失败、超时或中断 | 不自动重试；用户可手动刷新 | 不自动重试；若可能已出站，最多一次 GET 核对 |
| HTTP 408 / 5xx | 不自动重试 | 不重放；最多一次 GET 核对 |
| HTTP 429 | 不自动重试 | 不自动重放；提示稍后手动操作 |
| HTTP 401 | 不重试；清除本端 credential | 不重试；清除本端 credential |
| HTTP 403 / 其他明确业务拒绝 | 不重试 | 不重试，不额外伪造成功 |
| 已开启 / 已关闭 | 更新已知状态 | 本地或控制前 GET 直接拒绝，不发 POST |

当前实现不会把 `Retry-After` 响应头转为界面倒计时。因为没有自动重试，它不会据此自动发包；用户手动重试仍受本地冷却与服务端限流约束。

## 会话失效消息不计入 HTTP

手机的 `/session/clear` 与手表的 `/session/invalid` 是 secret-free Wear Message，不是官方网络请求。发送端先清除自己，再枚举当时已连接节点；每节点最多一次发送，没有 ACK、重试或离线队列。它只能 best-effort 提醒另一端，不能被描述成跨设备原子注销。

离线一端可能继续持有旧 token。恢复连接不会自动补发旧消息；该端在下一次官方 401 时自行清除，或由用户重新同步/主动清除。`credentialId` 精确匹配只防止迟到旧消息清掉新代际会话。

## 本轮验收边界

v0.2 本轮没有安装、真实网络、OTP、状态 GET 或控制 POST。本页的请求形态来自当前实现与协议测试设计；当前单元测试为 core 17/0、mobile 9/0、Wear Fake 9/0、Wear Real 9/0，完整构建与 lint 结果见 [TEST_REPORT.md](TEST_REPORT.md)。
