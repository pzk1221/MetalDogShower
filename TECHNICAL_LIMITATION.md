# 技术限制与未验证项

## 无 Refresh Token，也没有固定 5 分钟 TTL

官方 Android APK 4.1.52 只保存一个 `token`，没有发现 refresh token、刷新端点或轮换逻辑；401 会清除 token 并返回登录。因此 v0.2 可以持久保留手机/手表各自的本地密文，却不能保证一次 OTP 永久有效。

正确行为是让现有 token 一直使用到官方返回 401、用户主动退出或本地密文不可恢复，再在手机重新完成短信验证码登录。任何“自动刷新”或“token 必然 5 分钟失效”的说法都没有证据。同步流程的 60 秒总超时只限制一次 Data Layer 传输，不限制 token 生命周期。

## v0.2 生产回归尚未执行

用户曾反馈上一版在现场成功开启和关闭，说明上一版至少在当时的账号、设备与服务状态下可工作；但它不是 v0.2 的可重复测试记录，也没有保留足以分析所有响应字段与精确请求数量的脱敏日志。

v0.2 本轮按用户要求只制作与编译准备，没有安装到手机/手表，没有真实网络、OTP、状态 GET 或控制 POST。双浴室、手机持久登录、手机控制、小组件、credentialId 代际和双向失效都必须在以后获得用户明确授权时做最小现场回归，不能把“代码已实现”写成“生产已验证”。

## 会员状态字段仍未知

`GET user/0/0/protect/getInfo` 已从官方实现确认，但生产响应中哪个字段代表有效会员、是否有场馆范围、余额或时间段限制仍没有可靠记录。UI 不能仅因为本地有 token 就显示“会员有效”；受保护状态/控制接口仍由服务端做最终授权。

## 手机和手表共享 token，不共享原子会话

两端保存的是同一个官方 token 值，但分别由各自 AndroidKeyStore 密钥加密。服务端没有已验证的“列出会话 / 撤销某端 / refresh / logout”端点，因此本地删除不能撤销服务端 token，也不能同步提交成一项跨设备事务。

手机 `/session/clear` 和手表 `/session/invalid` 只对发送时已连接节点做一轮 best-effort，每节点最多尝试一次，没有 ACK、重试或离线队列。离线端可能保留旧 token。`credentialId` 精确匹配只能防止迟到旧通知误删新会话，不能让离线注销原子化。

如果手机修改浴室配置，会保留同一 token、轮换 credentialId、标记手表未同步，并尝试清除旧代际手表配置。通知失败时，旧手表仍可能暂时持有旧路由；在重新同步前不得依赖它与手机配置一致。

## 跨设备并发无法完全阻止

手机主界面与小组件共享手机进程锁，手表也有本地锁和 RequestGate，但两台设备之间没有分布式锁，官方 switch 接口也没有已验证的 Idempotency-Key。两个客户端同时控制同一浴室可能产生相互覆盖、错误的状态推断或额外出水。

因此强制使用规则是：**不得同时用手机和手表控制同一间浴室。** 操作前必须确认所选槽位和现场状态；不明确结果出现后不得换另一台设备立即重试。

## 双浴室路由限制

- 只支持槽位 1 和 2，共 1–2 间浴室，不支持任意数量。
- 两个槽位不得复用同一 canonical `deviceId`，因为官方 switch POST 只携带 deviceId。
- 两间浴室共享同一个官方账号 token 和同一客户端级控制冷却；不是两套独立账号。
- 切换槽位必须清除/忽略上一浴室的已知状态，不能把浴室1状态用于浴室2。
- 未配置槽位只能回手机主应用完成设置。

## Provisioning 版本兼容

Data Layer 外层仍为 v1：路径、`version=1` 和 AAD 前缀都没有变。加密明文 schema 为 v2，包含 credentialId 与 1–2 个 `devices`。把“内层 v2”误写成“整个传输协议 v2”会破坏兼容性说明。

Watch 解析器接受旧内层 v1 单设备明文并规范化为槽位1，同时生成/记住一个本地 credentialId；新手机只发送内层 v2。旧配置不会自动改写原密文，只有重新同步才获得完整双浴室 schema。

## 小组件的安全取舍

小组件不后台刷新，也不在广播接收器中发送控制。它展示的是最近一次已确认的非敏感镜像，可能因进程死亡、网络变化或另一设备操作而过期。

为降低风险，小组件只有在本地状态已知且确认时间不超过 2 分钟时才允许进入控制确认页；用户确认后仍执行一次新鲜路由 GET。这个 2 分钟只限制小组件显示状态的控制资格，不是 token TTL，也不保证期间没人从手表或官方 App 改变状态。

缺少登录、未配置、UNKNOWN、过期或控制桥不可用时，小组件打开主应用。执行中 Activity 被重建会把结果标记为不明确；用户必须现场确认并手动刷新，不能重复点击。

## 控制结果不明确

控制 POST 超时或连接中断不能证明服务端没有执行。项目不自动重复 POST，只允许一次状态 GET 核对。核对仍失败时该路由进入 UNKNOWN 锁定态；进程死亡或取消若发生在 POST 可能出站之后，也必须按不明确处理。

这能降低重复开关风险，但不能从客户端证明物理阀门状态。现场观察仍是最终安全边界。

## 二维码 URL 当前 404

二维码载荷在研究阶段从 Aliyun OSS 返回 `NoSuchKey`，没有 HTTP 重定向。微信仍能打开小程序，最合理解释是微信普通链接二维码到小程序的客户端映射。自制客户端不依赖这条明文 HTTP 链，只在本地取允许 host 的路径短码，再调用官方 HTTPS 公开设备接口。

## Retry-After 展示

服务端返回 429 时客户端不会自动重试，并提示用户稍后手动操作。当前版本没有把 `Retry-After` 响应头转换为可见倒计时；后续若实现，只能延长本地禁用时间，不能增加自动请求。

## 网络与系统休眠

手机或手表没有 INTERNET capability 时不控制；Samsung/Android 对“已验证互联网”的判断可能与实际 Wi-Fi 可用性短暂不一致。当前客户端只要求 active network 声明 INTERNET，最终以一次显式 HTTPS 请求为准，不修改系统代理或强行绕过 TLS。

应用没有常驻前台服务、永久 WakeLock、WebSocket 或后台轮询。倒计时不依赖每秒服务器请求；系统休眠期间不会维持网络任务，重新进入界面后由用户刷新校准。

## JVM 内存清除限制

敏感值尽量以可覆盖 `ByteArray` 持有，并在完成、失败或生命周期结束时覆盖。不过 JSON 解析、Kotlin/Java 数据类与 OkHttp header API 会短暂创建不可原地覆盖的 JVM `String`。项目能避免持久明文和日志、缩短驻留时间，但不能保证垃圾回收前内存中绝无残留副本。root 或已攻破的解锁设备也超出 AndroidKeyStore 能完全保护的威胁边界。

## 非官方客户端与服务条款

官方 APK 的公开分发和静态协议实现可以验证技术兼容性，但不等于服务方明确授权第三方客户端。项目只面向用户本人、低频正常使用；若服务条款或服务方要求只能使用官方客户端，应停止 Real 模式。

## 待验证清单

- v0.2 单元测试当前为 core 17/0、mobile 9/0、Wear Fake 9/0、Wear Real 9/0；最终构建/lint 矩阵见测试报告。
- v0.2 手机上的双浴室配置、持久会话恢复和手机控制：未在本轮实机执行。
- v0.2 日/夜小组件在目标手机 Launcher 上的尺寸、主题与确认流程：未在本轮实机执行。
- v0.2 手机到 Watch 的内层 provisioning v2 双浴室同步：未在本轮实机执行。
- v0.2 双向 401/退出通知在在线、离线与迟到消息场景下的实机收敛：未在本轮执行。
- token 实际寿命、会员业务字段和服务端完整错误语义：仍未知。
